export class PageStateModel {
  page: number;
  term: string;
  category: number;
  capacity: number;
  expiredStock: string;
  notForSale: boolean;
  saveState: boolean;
}
export class SortBatch {
  active?: String;
  direction?: String;
}
